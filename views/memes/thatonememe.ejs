<%- include('../partials/head') %>
<main class="container text-center mt-5">
    <div class="container bg-light text-center w-50 mt-5">
        <h1 class="bg-light">Look at this Meme!</h1>
    </div>
    <div class="container mt-3">
        <img src="<%= meme.memeUrl %>" alt="Current Meme" >
        <h2 class="bg-light mt-3">Rated: 
            <% if(meme.rating){ %>
            <%= meme.rating %> 
            <% } else { %> 
                ? 
            <% } %>  out of 5!</h2>
                <a class="btn btn-info btn-lg text-light" href="/memes/<%= meme.id %>/rate">Rate</a>
    </div>
    <div class="container mt-5">
        <form action="/">
            <button class="btn btn-secondary btn-lg">Go Back Home</button>
        </form>
    </div>
</main>
<%- include('../partials/foot') %>